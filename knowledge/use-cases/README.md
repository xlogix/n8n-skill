# Use Cases

Workflow intent mapping for fast node selection.

Primary file:

- `workflow-needs.json`: machine-queryable mapping from workflow goals to capabilities, groups, and recommended starter nodes.

Use with:

- `/docs/reference/indexes/nodes-by-capability.json`
- `/docs/reference/indexes/nodes-by-group.json`
- `/docs/reference/node-catalog/nodes/*.md`
