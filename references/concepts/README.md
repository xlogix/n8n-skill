# Concepts

Use these documents before selecting nodes. They define how n8n executes data and evaluates expressions.

- `execution-model.md`: run lifecycle, item flow, branching behavior
- `data-contracts.md`: JSON/binary payload shape and node interface assumptions
- `expressions.md`: expression safety and deterministic data access
